term_path = "/dev/termination-log"
            if os.path.exists(term_path):
                try:
                    with open(term_path, "r") as f:
                        content = f.read().strip()
                        if content:
                            print(f"Previous termination log content:\n{content}", flush=True)
                        else:
                            print("/dev/termination-log exists but empty", flush=True)
                except Exception as e:
                    print(f"Could not read /dev/termination-log: {e}", flush=True)
            else:
                print("️No /dev/termination-log file found", flush=True)