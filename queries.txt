   // Search records by query and team with pagination
    @Query(value = "SELECT * FROM incident_tracker i " +
            "WHERE i.team = :team " +
            "AND (UPPER(i.incident_number) LIKE UPPER(CONCAT('%', :query, '%')) " +
            "OR UPPER(i.customer_exp) LIKE UPPER(CONCAT('%', :query, '%')) " +
            "OR UPPER(CAST(i.analysis AS VARCHAR)) LIKE UPPER(CONCAT('%', :query, '%'))) " +
            "ORDER BY i.create_datetime DESC",
    nativeQuery = true)
    Page<IncidentTracker> searchByQueryAndTeam(@Param("query") String query, 
                                        @Param("team") String team, 
                                        Pageable pageable);
=====replace to

@Query(value = "SELECT * FROM incident_tracker i " +
        "WHERE i.team = :team " +
        "AND (" +
        "UPPER(i.incident_number) LIKE UPPER('%' || :query || '%') " +
        "OR UPPER(DBMS_LOB.SUBSTR(i.customer_exp, 4000, 1)) LIKE UPPER('%' || :query || '%') " +
        "OR UPPER(DBMS_LOB.SUBSTR(i.analysis, 4000, 1)) LIKE UPPER('%' || :query || '%')" +
        ") " +
        "ORDER BY i.create_datetime DESC",
    nativeQuery = true)
Page<IncidentTracker> searchByQueryAndTeam(@Param("query") String query, 
                                           @Param("team") String team, 
                                           Pageable pageable);


@Query(value = "SELECT * FROM incident_tracker i " +
        "WHERE i.team = :team " +
        "AND (" +
        "UPPER(i.incident_number) LIKE UPPER('%' || :query || '%') " +
        "OR UPPER(DBMS_LOB.SUBSTR(i.customer_exp, 4000, 1)) LIKE UPPER('%' || :query || '%') " +
        "OR UPPER(DBMS_LOB.SUBSTR(i.analysis, 4000, 1)) LIKE UPPER('%' || :query || '%')" +
        ") " +
        "ORDER BY i.create_datetime DESC",
    nativeQuery = true)
Page<IncidentTracker> searchByQueryAndTeam(@Param("query") String query, 
                                           @Param("team") String team, 
                                           Pageable pageable);


============

// Search records by query with pagination
    @Query(value = "SELECT * FROM incident_tracker i " +
            "WHERE (UPPER(i.incident_number) LIKE UPPER(CONCAT('%', :query, '%')) " +
            "OR UPPER(i.customer_exp) LIKE UPPER(CONCAT('%', :query, '%')) " +
            "OR UPPER(CAST(i.analysis AS VARCHAR)) LIKE UPPER(CONCAT('%', :query, '%'))) " +
            "ORDER BY i.create_datetime DESC",
    nativeQuery = true)
    Page<IncidentTracker> searchByQueryWithoutTeam(@Param("query") String query,                                         
                                        Pageable pageable);
    
===replace to oracle

@Query(value = "SELECT * FROM incident_tracker i " +
        "WHERE (UPPER(i.incident_number) LIKE UPPER('%' || :query || '%') " +
        "OR UPPER(DBMS_LOB.SUBSTR(i.customer_exp, 4000, 1)) LIKE UPPER('%' || :query || '%') " +
        "OR UPPER(DBMS_LOB.SUBSTR(i.analysis, 4000, 1)) LIKE UPPER('%' || :query || '%')) " +
        "ORDER BY i.create_datetime DESC",
nativeQuery = true)
Page<IncidentTracker> searchByQueryWithoutTeam(@Param("query") String query, Pageable pageable);

=========================
    // Dashboard Landing Page Search Criteria 
    @Query(value = "SELECT * FROM incident_tracker i " +
            "WHERE i.team = :team " +
            "AND (UPPER(i.incident_number) LIKE UPPER(CONCAT('%', :query, '%')) " +
            "OR UPPER(i.customer_exp) LIKE UPPER(CONCAT('%', :query, '%')) " +
            "OR UPPER(CAST(i.analysis AS VARCHAR)) LIKE UPPER(CONCAT('%', :query, '%')) " +
            "OR LOWER(i.alert_name) LIKE LOWER(CONCAT('%', :query, '%'))) " +
            "ORDER BY i.create_datetime DESC", nativeQuery = true)
    List<IncidentTracker> findIncidentsBySearchCriteria(@Param("query") String query, @Param("team") String team);
	
=====replace to
@Query(value = "SELECT * FROM incident_tracker i " +
        "WHERE i.team = :team " +
        "AND (UPPER(i.incident_number) LIKE UPPER('%' || :query || '%') " +
        "OR UPPER(DBMS_LOB.SUBSTR(i.customer_exp, 4000, 1)) LIKE UPPER('%' || :query || '%') " +
        "OR UPPER(DBMS_LOB.SUBSTR(i.analysis, 4000, 1)) LIKE UPPER('%' || :query || '%') " +
        "OR LOWER(DBMS_LOB.SUBSTR(i.alert_name, 4000, 1)) LIKE LOWER('%' || :query || '%')) " +
        "ORDER BY i.create_datetime DESC", 
nativeQuery = true)
List<IncidentTracker> findIncidentsBySearchCriteria(@Param("query") String query, @Param("team") String team);

=========chat queries

@Query(value = "SELECT * FROM incident_tracker i " +
            "WHERE i.team = :team " +
            "AND (UPPER(i.incident_number) LIKE UPPER(CONCAT('%', :query, '%')) " +
            "OR UPPER(i.customer_exp) LIKE UPPER(CONCAT('%', :query, '%')) " +
            "OR UPPER(CAST(i.analysis AS VARCHAR)) LIKE UPPER(CONCAT('%', :query, '%'))) " +
            "ORDER BY i.create_datetime DESC LIMIT 5",
    nativeQuery = true)
    List<ChatIncident> searchByQueryAndTeam(@Param("query") String query, @Param("team") String team);
	
============ replace to

@Query(value = "SELECT * FROM incident_tracker i " +
        "WHERE i.team = :team " +
        "AND (UPPER(i.incident_number) LIKE UPPER('%' || :query || '%') " +
        "OR UPPER(DBMS_LOB.SUBSTR(i.customer_exp, 4000, 1)) LIKE UPPER('%' || :query || '%') " +
        "OR UPPER(DBMS_LOB.SUBSTR(i.analysis, 4000, 1)) LIKE UPPER('%' || :query || '%')) " +
        "ORDER BY i.create_datetime DESC " +
        "FETCH FIRST 5 ROWS ONLY",
nativeQuery = true)
List<ChatIncident> searchByQueryAndTeam(@Param("query") String query, @Param("team") String team);

==============

    @Query(value = "SELECT * FROM incident_tracker i " +
            "WHERE i.team = :team " +
            "AND (UPPER(i.customer_exp) LIKE UPPER(CONCAT('%', :query, '%')) " +
            "OR UPPER(CAST(i.analysis AS VARCHAR)) LIKE UPPER(CONCAT('%', :query, '%')) " +
            "OR UPPER(i.alert_name) LIKE UPPER(CONCAT('%', :query, '%'))) " +
            "ORDER BY i.create_datetime DESC LIMIT 100",
        nativeQuery = true)
    List<ChatIncident> fetchIncidents(@Param("query") String query, @Param("team") String team);
	
===============replace to


@Query(value = "SELECT * FROM incident_tracker i " +
        "WHERE i.team = :team " +
        "AND (" +
        "UPPER(DBMS_LOB.SUBSTR(i.customer_exp, 4000, 1)) LIKE UPPER('%' || :query || '%') " +
        "OR UPPER(DBMS_LOB.SUBSTR(i.analysis, 4000, 1)) LIKE UPPER('%' || :query || '%') " +
        "OR UPPER(DBMS_LOB.SUBSTR(i.alert_name, 4000, 1)) LIKE UPPER('%' || :query || '%')" +
        ") " +
        "ORDER BY i.create_datetime DESC " +
        "FETCH FIRST 100 ROWS ONLY",
    nativeQuery = true)
List<ChatIncident> fetchIncidents(@Param("query") String query, @Param("team") String team);


=============

@Query(value = "SELECT * FROM incident_tracker i " +
                   "WHERE i.team = :team " +
                   "AND (UPPER(i.customer_exp) LIKE UPPER(CONCAT('%', :query, '%')) " +
                   "OR UPPER(i.analysis) LIKE UPPER(CONCAT('%', :query, '%')) " +
                   "OR UPPER(i.alert_name) LIKE UPPER(CONCAT('%', :query, '%'))) " +
                   "ORDER BY i.create_datetime DESC " +
                   //"FETCH FIRST 100 ROWS ONLY",
                   "LIMIT 100",
           nativeQuery = true)
    List<ChatIncident> fetchIncidentsJava(@Param("query") String query, @Param("team") String team);	



============replace to

@Query(value = "SELECT * FROM incident_tracker i " +
        "WHERE i.team = :team " +
        "AND (" +
        "UPPER(DBMS_LOB.SUBSTR(i.customer_exp, 4000, 1)) LIKE UPPER('%' || :query || '%') " +
        "OR UPPER(DBMS_LOB.SUBSTR(i.analysis, 4000, 1)) LIKE UPPER('%' || :query || '%') " +
        "OR UPPER(DBMS_LOB.SUBSTR(i.alert_name, 4000, 1)) LIKE UPPER('%' || :query || '%')" +
        ") " +
        "ORDER BY i.create_datetime DESC " +
        "FETCH FIRST 100 ROWS ONLY",
    nativeQuery = true)
List<ChatIncident> fetchIncidentsJava(@Param("query") String query, @Param("team") String team);


======================

@Query(value = "SELECT * FROM incident_tracker i " +
	            "WHERE i.team = :team " +
	            "AND (" +
	            "UPPER(i.customer_exp) LIKE UPPER('%' || :word1 || '%') " +
	            "OR UPPER(i.analysis) LIKE UPPER('%' || :word1 || '%') " +
	            "OR UPPER(i.alert_name) LIKE UPPER('%' || :word1 || '%') " +
	            "OR UPPER(i.customer_exp) LIKE UPPER('%' || :word2 || '%') " +
	            "OR UPPER(i.analysis) LIKE UPPER('%' || :word2 || '%') " +
	            "OR UPPER(i.alert_name) LIKE UPPER('%' || :word2 || '%')" +
	            ") " +
	            "ORDER BY i.create_datetime DESC " +
	            "LIMIT 100",
	    nativeQuery = true)
	List<ChatIncident> fetchIncidentsSql(@Param("word1") String word1, @Param("word2") String word2, @Param("team") String team);

===========replace to

@Query(value = "SELECT * FROM incident_tracker i " +
        "WHERE i.team = :team " +
        "AND (" +
        "UPPER(DBMS_LOB.SUBSTR(i.customer_exp, 4000, 1)) LIKE UPPER('%' || :word1 || '%') " +
        "OR UPPER(DBMS_LOB.SUBSTR(i.analysis, 4000, 1)) LIKE UPPER('%' || :word1 || '%') " +
        "OR UPPER(DBMS_LOB.SUBSTR(i.alert_name, 4000, 1)) LIKE UPPER('%' || :word1 || '%') " +
        "OR UPPER(DBMS_LOB.SUBSTR(i.customer_exp, 4000, 1)) LIKE UPPER('%' || :word2 || '%') " +
        "OR UPPER(DBMS_LOB.SUBSTR(i.analysis, 4000, 1)) LIKE UPPER('%' || :word2 || '%') " +
        "OR UPPER(DBMS_LOB.SUBSTR(i.alert_name, 4000, 1)) LIKE UPPER('%' || :word2 || '%')" +
        ") " +
        "ORDER BY i.create_datetime DESC " +
        "FETCH FIRST 100 ROWS ONLY",
    nativeQuery = true)
List<ChatIncident> fetchIncidentsSql(@Param("word1") String word1, @Param("word2") String word2, @Param("team") String team);

===============without team

Query(value = "SELECT * FROM incident_tracker i " +
            "WHERE (" +
            "UPPER(i.customer_exp) LIKE UPPER('%' || :word1 || '%') " +
            "OR UPPER(i.analysis) LIKE UPPER('%' || :word1 || '%') " +
            "OR UPPER(i.alert_name) LIKE UPPER('%' || :word1 || '%') " +
            "OR UPPER(i.customer_exp) LIKE UPPER('%' || :word2 || '%') " +
            "OR UPPER(i.analysis) LIKE UPPER('%' || :word2 || '%') " +
            "OR UPPER(i.alert_name) LIKE UPPER('%' || :word2 || '%')" +
            ") " +
            "ORDER BY i.create_datetime DESC " +
            "LIMIT 100",
    nativeQuery = true) 
    List<ChatIncident> fetchIncidentsSql(@Param("word1") String word1, @Param("word2") String word2);
	
==========replace to

@Query(value = "SELECT * FROM incident_tracker i " +
        "WHERE (" +
        "UPPER(DBMS_LOB.SUBSTR(i.customer_exp, 4000, 1)) LIKE UPPER('%' || :word1 || '%') " +
        "OR UPPER(DBMS_LOB.SUBSTR(i.analysis, 4000, 1)) LIKE UPPER('%' || :word1 || '%') " +
        "OR UPPER(DBMS_LOB.SUBSTR(i.alert_name, 4000, 1)) LIKE UPPER('%' || :word1 || '%') " +
        "OR UPPER(DBMS_LOB.SUBSTR(i.customer_exp, 4000, 1)) LIKE UPPER('%' || :word2 || '%') " +
        "OR UPPER(DBMS_LOB.SUBSTR(i.analysis, 4000, 1)) LIKE UPPER('%' || :word2 || '%') " +
        "OR UPPER(DBMS_LOB.SUBSTR(i.alert_name, 4000, 1)) LIKE UPPER('%' || :word2 || '%')" +
        ") " +
        "ORDER BY i.create_datetime DESC " +
        "FETCH FIRST 100 ROWS ONLY",
    nativeQuery = true)
List<ChatIncident> fetchIncidentsSql(@Param("word1") String word1, @Param("word2") String word2);


==================	
    
    @Query(value = "SELECT * FROM incident_tracker i " +
               "WHERE (UPPER(i.customer_exp) LIKE UPPER(CONCAT('%', :query, '%')) " +
               "OR UPPER(i.analysis) LIKE UPPER(CONCAT('%', :query, '%')) " +
               "OR UPPER(i.alert_name) LIKE UPPER(CONCAT('%', :query, '%'))) " +
               "ORDER BY i.create_datetime DESC " +
               "LIMIT 100",
       nativeQuery = true)
	List<ChatIncident> fetchIncidentsSql(@Param("query") String query);


=======replace to

	

@Query(value = "SELECT * FROM incident_tracker i " +
        "WHERE (" +
        "UPPER(DBMS_LOB.SUBSTR(i.customer_exp, 4000, 1)) LIKE UPPER('%' || :query || '%') " +
        "OR UPPER(DBMS_LOB.SUBSTR(i.analysis, 4000, 1)) LIKE UPPER('%' || :query || '%') " +
        "OR UPPER(DBMS_LOB.SUBSTR(i.alert_name, 4000, 1)) LIKE UPPER('%' || :query || '%')" +
        ") " +
        "ORDER BY i.create_datetime DESC " +
        "FETCH FIRST 100 ROWS ONLY",
    nativeQuery = true)
List<ChatIncident> fetchIncidentsJava(@Param("query") String query);
